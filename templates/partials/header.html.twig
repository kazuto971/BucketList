<header class="bg-dark text-white p-3">
    <div class="container d-flex align-items-center">
        <img src="{{ asset('images/logo.png') }}" alt="Logo" height="40" class="me-3">
        <h1 class="h4 m-0">Bucket-List</h1>
    </div>
</header>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_homme') }}">Accueil</a>
        <a class="nav-link" href="{{ path('app_about') }}">About us</a>
        <a class="nav-link" href="{{ path('wish_list') }}">Wishes</a>
        <a class="nav-link" href="{{ path('wish_add') }}">New Wish</a>
    </div>
</nav>
